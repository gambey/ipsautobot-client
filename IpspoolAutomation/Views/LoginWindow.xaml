<Window x:Class="IpspoolAutomation.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="登录 - 欢迎使用IPs Autobot自动化平台" Height="320" Width="380"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>

            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="账号/手机号" Margin="0,0,0,4"/>
        <TextBox Grid.Row="1" Text="{Binding Account, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" Padding="6,4"/>
        <TextBlock Grid.Row="2" Text="密码" Margin="0,0,0,4"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Margin="0,0,0,8" Padding="6,4"/>
        <TextBlock Grid.Row="4" Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" VerticalAlignment="Top"/>
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <!--Button Content="注册" Command="{Binding OpenRegisterCommand}" Margin="0,0,8,0" Padding="12,6"/-->
            <!--Button Content="注册" Command="{Binding OpenRegisterCommand}" Margin="0,0,8,0" Padding="12,6"/-->
            <!--Button Content="注册" Command="{Binding OpenRegisterCommand}" Margin="0,0,8,0" Padding="12,6"/-->
            <Button Content="登录" x:Name="LoginButton" Click="LoginButton_Click" IsDefault="True" Padding="12,6" Background="#FF272829" Foreground="#FFF5F0F0"/>
        </StackPanel>
        <Button Content="修改密码" Command="{Binding OpenChangePasswordCommand}" Padding="12,6" Margin="-1,0,224,0" Grid.Row="5"/>
    </Grid>
</Window>
